[build]
  # Directory (relative to root of your repo) that contains the deploy-ready
  # HTML files and assets generated by the build. If a base directory has
  # been specified, include it in the publish directory path.
  publish = "dist/client"

  # Directory with the serverless Lambda functions to deploy to AWS.
  functions = "dist/functions"

  # Default build command.
  command = "npm run dry-compile && npm run build"

[dev]
  # Command to start your dev server
  command = "npm run serve:client"

  # Port that the dev server will be listening on
  port = 3000

  # functionsPort = 34567 # port for functions server
  targetPort = 8080 # Port of target app server

  publish = "dist/client"

  functions = "dist/functions"

  autoLaunch = true # a Boolean value that determ
